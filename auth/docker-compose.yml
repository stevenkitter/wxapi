version: "2"

services:
  wxapi_auth:
    image: coderxu/wxapi_auth
    networks:
      - internal
    environment:
      MICRO_REGISTRY: "mdns"
      MICRO_SERVER_ADDRESS: ":50051"
      MysqlURL: mariadb:3306
networks:
  internal:
    external: true
